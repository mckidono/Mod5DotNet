@using Mod_5_Northwind.Models
@model IEnumerable<Category>

<div class="bg-primary m-1 p-1 text-white"><h4>Product Listing</h4></div>
    @foreach (Category category in Model)
    { 
        <li>@category.CategoryName</li>
    }

